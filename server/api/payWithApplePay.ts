import type { IncomingMessage, ServerResponse } from 'http'

// this is where we get apple pay token and could go to checkout to bill
export default async (req: IncomingMessage, res: ServerResponse) => {
    // console()
}